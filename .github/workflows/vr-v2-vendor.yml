name: VR V2 Build Gate

on:
  pull_request:
    branches:
      - main
    paths-ignore:
      - 'vr-v2-payload/**'

permissions:
  contents: read

jobs:
  idle:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Payload upload gate ready'
